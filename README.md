# PSITE Mastery

A Streamlit quiz / review app for pediatric surgery topics.

## Quick start

```bash
python -m venv venv
source venv/bin/activate   # Windows: venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py
